import { Layout } from "../../components/Layout";
import { Settings } from "../../components/Settings";

export const SettingsPagePresenter = () => {
  return (
    <Layout>
      <Settings />
    </Layout>
  );
};
