import { Environment } from "../../components/Environment";
import { Layout } from "../../components/Layout";

export const EnvironmentPagePresenter = () => {
  return (
    <Layout>
      <Environment />
    </Layout>
  );
};
