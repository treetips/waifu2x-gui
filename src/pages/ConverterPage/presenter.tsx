import { FileContainer } from "../../components/FileContainer";
import { Layout } from "../../components/Layout";

export const ConverterPagePresenter = () => {
  return (
    <Layout>
      <FileContainer />
    </Layout>
  );
};
